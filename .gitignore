# Dependencies
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz

# Testing
coverage/

# Next.js
.next/
out/
build/
dist/

# PWA build artifacts (generated by next build via @ducanh2912/next-pwa)
public/sw.js
public/workbox-*.js
public/fallback-*.js
public/worker-*.js

# Production
*.tsbuildinfo
next-env.d.ts

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Local env files
.env
.env*.local
.env.development
.env.production

# Vercel
.vercel

# IDE
.idea/
*.swp
*.swo
.vscode/
!.vscode/extensions.json

# Claude Code per-developer artifacts. settings.json carried allow/deny
# permission lists that drift per machine; tracking it caused merge churn,
# so we treat it the same as settings.local.json now.
.claude/settings.json
.claude/settings.local.json

# Tokyo Met open-data CSVs cached by scripts/generate-pois.ts.
# Tokyo-wide files (避難所 ~13MB) are too large for git; the script refetches
# from the upstream CKAN-resolved URL on demand, then filters down to Koto-ku.
__fixtures__/opendata/tokyo-*.csv

# Datasets now served live via /api/datasets/{aed,toilet,events,gomi}.
# If a developer runs the lib helpers locally for debugging and writes the
# output to disk, don't let it sneak back into git. wbgt.json was on the
# same pipeline; ignore it too.
data/aed.json
data/toilet.json
data/events.json
data/gomi.json
data/wbgt.json

# Generated at `predev`/`prebuild`/`pretest` by scripts/ensure-data.mjs.
# Curated files (gomi-dictionary.json, gomi-schedule.json) stay tracked
# because they have no upstream source.
data/districts.json
data/shelter.json
data/assembly_point.json
data/water_supply.json
data/park.json
data/library.json
data/child_center.json
data/nursery.json
data/bus-toei.json
